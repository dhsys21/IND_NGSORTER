# NGSORTER 생산 전 코드 검토

검토일: 2026-09-06. 기준: 로컬 main, `6cc352d` (`2026 09 06 001`).

실트레이 정상 동작 시험이 완료된 상태에서, 이동/취출/삽입, 수동 전환/재시작, 도어, PLC/CC-Link, 바코드, FMS/Gateway, 연기감지기 보고, 로컬 트레이 저장의 실패 경로를 검토했다. 설비 제어 소스는 수정하지 않았다. 이 검토는 소프트웨어 검토이며, 안전 회로 검증이나 실장비 고장 시험을 대체하지 않는다.

P1은 생산 투입 전에 우선 해결할 항목, P2는 예외 상황의 신뢰성을 위해 보완할 항목이다. 코드상 경로와 실제 발생 조건을 구분해 기록했다. Maximum speed 모드 관련 항목은 해당 옵션을 사용하는 경우에 해당한다.

## 주요 발견 사항

### 1. [P1] Pause가 이미 시작한 축 이동을 정지하지 않음

- 근거: [req_Pause](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:170), [수동 전환](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/FormMain.cpp:2110), [KeyLock 해제](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2881).
- 수동 전환 시 시퀀스만 `seqPause`로 변경한다. 이미 포지션 보드에 전달한 이동을 정지시키는 API는 호출하지 않는다. KeyLock 해제는 수동 모드와 BYPASS 접점만 확인하며, 모든 축의 정지 완료를 확인하지 않는다.
- 따라서 이동 중 수동 전환 후 키락을 해제하면 이전 이동이 끝나기 전에 도어 해제 출력이 나갈 수 있다. 외부 안전 회로가 실제로 정지시키는지는 별도로 검증해야 한다.
- 개선: 수동 전환 시 제어된 축 정지, 정지 완료 확인, 키락 해제 허가를 구분한다. 정지 후 재시작 시에는 중단한 이동 목표를 보존해야 한다. 기존 `req_Stop()`은 시퀀스도 바꾸므로 단순 교체하지 않는다.
- 검증: 실제 함수와 가짜 SSC API를 연결한 콘솔 테스트에서 Pause 후 이동 상태 유지와 Stop 호출 0회를 확인했다.

### 2. [P1] 새 좌표 설정이 실패해도 이전 좌표로 이동 명령을 실행할 수 있음

- 근거: [setPoint](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:893), [호출부](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:1179).
- `sscSetPointDataEx()` 실패 후에도 `sscAutoStart()`를 실행한다. 함수가 최종적으로 false를 반환하지만, X/Y/Z 이동 호출부는 그 반환값도 확인하지 않고 다음 단계로 넘어간다.
- 새 좌표 쓰기만 실패하고 시작 명령은 성공하면 보드에 남아 있던 이전 포인트가 실행될 수 있다. 한 축만 성공한 경우에도 정상 X/Y 이동처럼 대기 단계로 들어간다.
- 개선: 좌표 쓰기 실패 즉시 시작 명령을 차단한다. 호출부에서 각 축의 명령 성공을 확인하고, 부분 성공 시 동작을 중단하고 복구 대상으로 남긴다.
- 검증: 가짜 좌표 쓰기 실패를 주입했을 때 X/Y/Z/Z UP 모두 AutoStart가 1회 호출됨을 재현했다.

### 3. [P1] X0023 BUFFER가 충돌 정지 조건에 연결되지 않음

- 근거: [표시부](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/FormMain.cpp:2864), [AutoEject](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2115), [AutoInsert](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2354).
- `GRIPPER1_BUFFER`의 실제 사용처는 입력 정의와 화면/I/O 모니터 표시뿐이다. 취출/삽입의 외부 `nresult`는 0으로 시작하므로 기존 buffer 오류용 else 분기로 들어가지 않는다.
- BUFFER가 들어와도 이 입력 자체로 이동을 중단하거나 충돌 알람을 발생시키는 소프트웨어 처리가 없다.
- 개선: 센서의 실제 정상/이상 극성을 재확인한 뒤, 유효한 CC-Link 입력에서 BUFFER 이상을 공통 이동 정지와 명시적 복구 조건에 연결한다. 하드웨어 직접 정지 회로 유무도 확인한다.

### 4. [P1] 진행 중 CC-Link 통신 상실이 공통 이동 정지로 연결되지 않음

- 근거: [io_Read](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:139), [센서 타이머](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2616), [동작 호출](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2699).
- 읽기 실패 시 `m_ccLinkRunning=false`가 되지만 기존 입력값은 유지한다. 타이머는 반환값을 검사하지 않고 이후 동작 시퀀스를 실행한다. 개별 셀 감지 검사에는 통신 가드가 있으나 이동 중 전체 축을 정지시키는 공통 가드는 없다.
- 통신이 끊기기 직전의 도어/비상정지 접점이 정상값이면 기존 값이 계속 사용될 수 있다. SSC 보드는 별개이므로 PC 코드만으로 이동 정지를 보장하지 못한다.
- 개선: 운전 중 CC-Link 유효성 상실을 별도 알람으로 유지하고 축 정지/시퀀스 보류를 요청한다. 재연결만으로 자동 재시작하지 않으며, 입력 유효성 확인 후 작업자 재시작을 받는다.

### 5. [P1] Y003D를 서로 다른 타이머가 반대 정책으로 제어함

- 근거: [키락 설정 후 OFF](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2652), [BYPASS 키 상태 추종](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/FormMain.cpp:2887).
- 로봇 타이머는 키락 설정 후 500ms에 Y003D를 OFF한다. MainForm 타이머는 하드웨어 BYPASS 키가 ON인 동안 Y003D를 다시 ON한다.
- 키락 설정 후 작업자가 BYPASS 키를 OFF로 돌리기 전 구간에서 OFF가 유지되지 않고 다음 화면 타이머에 의해 덮어써진다. 안전 릴레이에 미치는 실제 영향은 회로 확인이 필요하지만, 코드의 출력 소유권 충돌은 명확하다.
- 개선: Y003D 목표값을 한 상태 머신에서만 결정한다. 화면 갱신은 표시만 하고 출력을 덮어쓰지 않게 한다. 현장에서 확인한 정상 도어/비상정지 복구 순서를 기준으로 전이표를 확정한다.

### 6. [P1] FMS가 TrackIn 데이터를 지우면 이미 삽입한 셀의 보고가 실패함

- 근거: [ReportCellTrackOut](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/Stage_mes.cpp:526), [READ_TRACK_IN_CELL](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:1351), [수신 캐시 갱신](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:980).
- 삽입 완료 후 보고 데이터를 로컬 확정 트레이 원본에서 가져오지 않고 현재 Gateway의 Location1 TrackIn 태그에서 다시 읽는다.
- 합의한 방식대로 FMS가 Response 초기화 시 TrackIn을 비우면 `CellExist=false` 또는 빈 CellId로 판단하여 보고를 거절한다. 데이터가 다른 값으로 변경된 경우에는 이미 전달받은 CellId를 현재 TrackIn 값으로 교체하기도 한다.
- 개선: TrayLoad 성공 시 원본 데이터를 TrayId/CellNo 기준의 변경 불가 스냅샷으로 보관하고, 모든 취출/삽입 보고와 재시도에 사용한다. 물리 취출 후 false로 바뀌는 `tray_source.CELL_EXIST`와 원본 데이터의 유효성을 혼용하지 않는다.
- 검증 조건: TrayLoadResponse=1 수신 후 Response=0과 동시에 TrackIn을 지우는 가짜 FMS 시나리오에서도 각 셀 보고가 유지되어야 한다.

### 7. [P1, Maximum speed 사용 시] 미완료 CellTrackOut을 다음 셀 보고가 덮어쓸 수 있음

- 근거: [다음 동작 우선 실행](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModGripper.cpp:832), [보고 결과 무시](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModGripper.cpp:848), [단일 pending 상태](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/Stage_mes.cpp:556), [단일 재시도 데이터](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:1489).
- 다음 취출을 시작한 뒤 보고하며, `ReportCellTrackOut()`의 false 반환도 처리하지 않는다. 동시에 미완료 거래 저장 공간은 한 건뿐이고 기존 pending 검사나 FIFO가 없다.
- FMS 응답이 다음 셀 삽입보다 늦으면 이전 셀의 payload/timeout/재시도 정보가 다음 셀로 바뀔 수 있다. Gateway 단절 시에는 보고하지 못한 채 다음 동작으로 진행할 수 있다.
- 개선: 보고 완료 전 다음 셀 진행을 막거나, 셀별 불변 payload를 저장하는 제한된 큐와 한 번에 한 건의 handshake를 구현한다. 큐 포화/전송 실패를 알람으로 처리한다. 검증 전 생산에서는 해당 옵션을 비활성화하는 것이 적절하다.

### 8. [P1] Gateway 전송 실패 시 pending 태그가 유실됨

- 근거: [전송 전 pending 삭제](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:1026), [전송 예외 처리](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:1128).
- JSON을 만들 때 pending 목록을 먼저 지운다. 그 뒤 연결이 끊기거나 유효한 수신 Context가 없어지거나 Write 예외가 발생해도 원래 pending을 복원하지 않는다.
- Request ON/OFF 또는 상태 변경이 유실되어 FMS timeout이나 양측 상태 불일치가 생긴다. 값이 변경될 때만 보고하는 EnvStatus는 같은 값으로 계속 수신되면 복구되지 않을 수 있다.
- 개선: 전송 배치에 순번을 부여하고 실패 시 보존/재전송한다. 재연결 시 EQP가 소유하는 상태를 재동기화한다. 소켓 Write 성공은 FMS 업무 처리 완료와 다르므로 handshake 재시도의 중복 처리 정책도 구분해야 한다.

### 9. [P1] 메인 스레드의 동기식 Gateway Write가 UI/제어 타이머를 지연시킬 수 있음

- 근거: [Alive 타이머](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:428), [동기 전송](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:1118), [작업 스레드의 동일 잠금 사용](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:508).
- VCL 직접 접근 방지는 적용되어 있지만, 메인 타이머가 `FSendLock`을 얻고 직접 `IOHandler->Write()`를 실행하는 구조는 남아 있다.
- 상대 Gateway가 연결만 유지하고 읽지 않는 경우 소켓 버퍼가 차면서 메인 스레드 Write 또는 같은 잠금 대기가 길어질 수 있다. 그동안 UI와 메인 스레드에서 실행되는 인터락 타이머도 지연된다.
- 개선: 네트워크 쓰기를 전용 송신 작업으로 옮기고 큐 길이/처리 시간/종료 취소를 제한한다. C++Builder 10.1 Indy에 없는 `WriteTimeout` 속성을 추가하는 방식은 사용하지 않는다.
- 현재 장비에서 Hang을 실재현한 것은 아니다. 실제 코드의 동기 I/O 경로와 차단 가능성을 확인한 항목이다.

### 10. [P2] 수동 모드 복귀 경로가 이미 성공한 FMS 거래를 재요청함

- 근거: [ResumeAutomaticFmsSequence](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/FormMain.cpp:1296), [CellTrackOut 재요청](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/FormMain.cpp:1315).
- Response=1을 수락하고 Request=0, Response=0을 기다리는 도중 수동 전환하면 pending은 남는다. AUTO 복귀 시에는 성공 결과와 `WaitResponseOff`를 지우고 같은 Request를 다시 ON한다.
- FMS 알람 복구 경로에서는 이미 수락한 결과를 보존하지만 일반 수동 복귀는 다르게 처리하므로 CellTrackOut/ProcessEnd 등의 중복 처리가 가능하다.
- 개선: 알람 재시도와 동일하게 거래 phase를 보존한다. 결과를 이미 받았으면 reset 완료만 기다리고, 아직 결과를 못 받은 경우만 합의한 재요청 정책을 적용한다.

### 11. [P2] 바코드 TCP 분할 수신을 완성된 TrayId로 처리함

- 근거: [ClientSocketBcrRead](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/Mod_SRX100W.cpp:184), [ProcessResult](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/Mod_SRX100W.cpp:196).
- CR/LF가 아직 없는 잔여 버퍼도 즉시 ProcessResult에 전달한다. 예를 들어 `TR-20260818-01A`가 `TR-2026`과 `0818-01A\r`로 나뉘면 앞부분을 먼저 ID로 수락하고 리딩 타이머를 종료할 수 있다.
- ProcessResult에는 현재 리딩 요청인지 확인하는 가드도 없어 timeout 이후 늦게 도착한 결과가 다시 수락될 수 있다. Source/Target 모두 같은 클래스의 영향을 받는다.
- 개선: 실제 리더 설정에 맞는 프레임 종료 조건을 확정하고 완성된 프레임만 처리한다. 트레이/리딩 요청의 세대 번호를 확인하고 잔여 버퍼와 최대 길이를 관리한다.

### 12. [P2] EnvStatus의 5개 값이 항상 하나의 일관된 배치라는 보장이 없음

- 근거: [PublishEnvStatus](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:865), [개별 태그 잠금](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:1096), [Snapshot 적용](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_Gateway.cpp:883).
- 5개 SetPcTag 호출이 각각 별도 잠금이다. 그 사이 Indy 작업 스레드가 BuildSuccessResponse를 호출하면 일부 필드만 먼저 배출될 수 있다. 같은 문제는 여러 필드로 구성된 업무 payload에도 적용된다.
- Gateway 재시작 때 수신 Snapshot으로 PC 태그 캐시를 대체하지만, 변경이 없는 마지막 EnvStatus를 강제로 다시 발행하는 경로도 없다.
- 개선: 전체 상태를 하나의 배치 API/잠금으로 갱신하고 송신 순서도 보장한다. 재연결 시 1회 전체 상태를 발행한 뒤 기존 변경 감지 정책을 유지한다.

### 13. [P2] PLC 바이너리 프레임 길이 검사와 버퍼 소비가 잘못되어 있음

- 근거: [7워드 읽기 설정](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModPLC_Bin.h:36), [수신 파서](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModPLC_Bin.cpp:158), [데이터 길이 계산](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModPLC_Bin.cpp:166).
- 현재 읽기 응답은 헤더 9바이트 + 종료코드 2바이트 + 7워드 14바이트로 25바이트, hex 문자열 50자다. 그런데 54자 이상이어야 파싱하므로 단독 정상 응답을 즉시 소비하지 못한다.
- 프레임 길이 계산에서도 hex 문자열 길이와 바이트 단위 데이터 길이를 혼합한다. 처리 후에는 소비한 프레임만 삭제하지 않고 버퍼 전체를 비운다. 수신 이벤트마다 읽기 완료 플래그를 세우므로 미완성 수신 중에도 다음 요청을 보낼 수 있다.
- 개선: 바이트 기반 누적 버퍼에서 헤더/길이/종료코드/전체 프레임을 검증한다. 완성된 프레임만 소비하고 남은 프레임과 조각은 유지한다. 실제 요청 길이와 응답 길이를 대조한다.
- 검증 조건: 정상 25바이트 단독 응답, 모든 위치에서의 분할 수신, 두 응답 합쳐 수신, 에러 응답, 불필요한 앞부분 데이터가 있는 응답.

### 14. [P2] 일부 동작 대기에 timeout이 없어 무기한 정지함

- 근거: [CHUCK 확인](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:2202), [X/Y 대기](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:1187), [Z DOWN 대기](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModRobostar.cpp:1274).
- CHUCK 입력이 들어오지 않으면 해당 step에서 계속 break하고 시간 제한이나 전용 실패 알람으로 넘어가지 않는다. 일부 위치 완료 대기에도 같은 누락이 있다.
- 공압 부족, 센서 고장, 명령 거절 등에서 작업자가 화면과 반복 로그만 보고 원인을 찾아야 한다.
- 개선: 대기 단계별 시작 시간, 제한 시간, 기대/현재 센서값을 기록하고 시간 초과를 복구 가능한 명시적 알람으로 처리한다. 반복 상태 로그는 변화 시 또는 제한된 주기로만 남긴다.

### 15. [P2] 중복 CellNo를 수락하여 이동 데이터와 보고 데이터가 달라질 수 있음

- 근거: [Source 검증](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:233), [트레이 적용](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:428), [보고용 검색](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModMes_OPCUA.cpp:1358).
- 범위와 필수 항목은 검사하지만 CellNo 중복은 검사하지 않는다. 같은 CellNo의 두 레코드가 있으면 트레이에는 마지막 레코드가 남고, READ_TRACK_IN_CELL은 첫 레코드를 반환한다.
- 개선: 사용하는 CellNo의 유일성을 TrayLoad 승인 전에 확인한다. 중복 CellId와 허용 Grade도 FMS 계약에 맞춰 검증한다. 테스트용 배열 인덱스 fallback은 생산 데이터의 잘못된 CellNo를 감추지 않도록 분리한다.

### 16. [P2] 로컬 트레이 저장 실패/중단 시 완료 플래그와 파일 상태가 달라질 수 있음

- 근거: [취출 완료 반영](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModGripper.cpp:178), [삽입 완료 반영](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/ModGripper.cpp:200), [파일 저장](D:/Project/2026/03.Jeng(India)/02.Program/02.NGSORTER/Stage_Form.cpp:477).
- 실제 작업 완료 후 메모리 완료 플래그를 먼저 설정하고 INI를 저장한다. 저장 실패를 호출자에게 성공/실패로 반환하는 계약이 없으며, 임시 파일 후 원자적 교체/직전 정상본 보존도 없다.
- 디스크 오류나 저장 중 프로그램 종료가 발생하면 실제 셀 위치, 메모리, 재시작 시 읽는 파일 사이의 일관성을 보장하기 어렵다.
- 개선: 임시 파일 저장/검증/교체 및 이전 정상본 보존을 적용한다. 저장 실패 시 다음 셀로 진행하지 않고, 물리 작업 완료와 FMS 보고 완료를 분리한 셀별 복구 기록을 남긴다.

## 확인한 기존 보호 기능

- X0022 active-low 셀 감지에는 CC-Link 정상 여부를 확인하는 가드가 있다. 통신 단절을 빈 그리퍼로 잘못 인정하지 않는 취출/삽입 개별 검사도 있다.
- 센터링 접점 상실/PLC 데이터 노후화에 대한 공통 이동 인터락은 `req_Stop()`을 호출하고 중단한 작업의 복구 정보를 보존한다.
- 일반 모드에서는 삽입 후 CellTrackOut handshake 완료 전 다음 셀 이동을 막는 경로가 있다.
- FMS 알람의 최종 Response=0 대기 재시도는 이미 수락한 결과를 유지한다. 일반 MANUAL 복귀와의 차이가 발견 사항 10이다.
- 정상 모드의 Request OFF -> Response=0 대기는 유지된다. 현재 Response=1/2를 바로 수락하는 정책은 `VERIFIED_BASELINE.md`에 명시된 승인 동작이므로, 이번 검토에서 임의로 과거 freshness 방식으로 되돌리자고 판단하지 않았다.

## 실행한 테스트와 한계

| 테스트 | 이번 검토 결과 |
| --- | --- |
| `tmp/FmsCurrentResponseFix/test_current_response.ps1` | 통과. 현재 Response 분류, 요청 시작, 최종 reset 재시도, Cycle Test 분기 |
| `tmp/FmsAlarmPauseClose/test_alarm_ui.ps1` | 통과. 알람 Pause/Retry/Close, 결과 보존, 10초 재표시 |
| `tmp/ProcessOrderFix/test_process_order.ps1` | 통과. STEP 순서, 32개 실제 입력 조합, 재시작 인터락 |
| `tmp/TrayWaitFix/test_tray_restart.ps1` | 통과. Pause 중 TrayLoad/바코드 보존, 중복 알림, 재시작 |
| `tmp/DoorPlcAuto/test_admission.ps1` | 테스트 하네스 컴파일 실패. 가짜 TMainForm에 최신 `opcTrayDisplayed`, `opcTrayLoaded`, `sourceCenteringCompleted`가 빠져 있음. 앱 빌드 실패가 아니라 테스트 유지보수 문제 |
| `tmp/Review20260906/test_motion.ps1` | 실제 함수 본문 + 가짜 SSC API로 발견 사항 1/2 재현. 위험 동작이 수정되었다는 PASS가 아니라 현재 문제 경로를 확인한 테스트 |

테스트는 Win32 bcc32 콘솔 프로그램이며 SSC 하드웨어 DLL이나 장비 프로그램을 실행하지 않았다. 전체 GUI 앱의 Win32 Debug 빌드는 이번 읽기 중심 검토에서는 다시 실행하지 않았다. 실제 통신 지연, 네트워크 단절, 센서 이상, 도어/비상정지 동작은 이번에 실기 검증하지 않았다. 기존 테스트 4개 통과만으로 위 경계 상황이 검증된 것은 아니다.

## 생산 전 검증 순서

1. 가짜 API/가짜 입력으로 이동 중 Pause, 좌표 쓰기 실패, BUFFER, CC-Link 상실에 대한 정지/복구 동작을 먼저 검증한다. 실기는 설비 담당자가 안전 조치와 회로를 확인한 시험 절차에서 수행한다.
2. 정상 도어 열기/닫기 및 비상정지 복구 동안 Y003C/Y003D/Y0033/Y0034와 실제 접점 전이를 함께 기록한다. 이동 정지 확인 전 도어 해제가 허용되지 않아야 한다.
3. 가짜 FMS에서 응답 지연/실패/초기화 지연을 주고, 각 handshake 단계의 수동 전환 및 재시작을 검증한다. 이미 완료된 셀을 다시 보고하거나 취출하면 안 된다.
4. TrayLoad 성공 후 TrackIn을 삭제해도 승인받은 셀 원본과 누적 TrackOut이 현재 Source 트레이 주기 동안 보존되는지 확인한다.
5. Gateway 재시작 및 수신 정지 상황에서 UI가 반응하고, pending 업무와 EnvStatus가 복구되는지 확인한다.
6. 바코드/PLC 가짜 서버로 분할/합쳐 수신과 늦은 응답을 검증한다.
7. 중복 CellNo, 빈 필수값, 허용하지 않은 Grade, 잘못된 CellCount는 이동 시작 전에 거절되는지 확인한다.
8. 테스트 저장 경로에서 저장 실패/중단을 주입하고, 재시작 시 실제 완료/미완료 상태와 보고할 셀을 구분할 수 있는지 확인한다.
9. 보완 후 전체 Win32 Debug 빌드와 기존 정상 실트레이 회귀 시험을 수행한다. 시험용 옵션은 생산 설정과 분리하고, 승인한 옵션 조합을 버전과 함께 기록한다.

권장 우선순위: 이동/도어 및 센서 이상 정지(1~5) -> FMS 데이터 보존/보고/통신 복구(6~12) -> PLC 프레임, timeout, 입력 검증, 저장 복구(13~16).
