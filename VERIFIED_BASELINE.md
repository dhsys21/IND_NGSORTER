# 실기 검증 기준 버전

## 최신 사용자 검증 기준 — 2026-09-08

- 프로그램 표시 버전: `Ver. 2026-09-08 001` (이번 병합 직전 로컬 수정본).
- 사용자가 "지금까지 수정한 내용은 테스트 완료했고 문제없이 잘 됐어"라고 확인함.
- 포함 변경: 수동 Source/Target Scan 및 TrayLoad 데이터 수신·표시·응답 초기화, 자동 시퀀스와 독립된 FMS Trouble 감시·Pause·알람·명시적 Retry/Restart.
- 수동 데이터 처리는 자동 취출·삽입/센터링/배출을 시작하지 않는다. FMS Trouble은 자동·수동 구분 없이 처리하며 신호 OFF만으로 자동 재개하지 않는다.
- 근거는 사용자 실설비 테스트 보고이며, 세부 시험 조합 전체가 제공된 것은 아니다.
- 노트북에서 먼저 push한 자동 Tray In 재검증 수정과 병합하기 **전** 기준이다. 병합 결과까지 실설비 검증됐다고 해석하지 않는다.

## 이전 사용자 검증 기준 — 2026-09-05

- 확인일: 2026-09-05
- 프로그램 표시 버전: `Ver. 2026-09-05 003`
- 검증 결과: 사용자가 현재 버전의 각 모드에서 테스트가 잘 되었다고 확인함.
- 근거: 사용자 실기 테스트 보고. 에이전트가 설비 시험을 직접 수행한 것은 아님.
- 범위: 각 모드 정상 동작 확인. 개별 시험 항목·속도·신호 조합의 상세 목록은 제공되지 않음.

## 기준 동작

- Pause 중 완료된 TrayLoad 응답과 도착한 바코드는 보관하고, Restart 후 다음 단계를 진행한다.
- Pause 자체를 실제 취출·삽입 작업으로 오판해 대상트레이 로드 요청을 누락하지 않는다. 실제 진행 중인 작업의 예약은 보호한다.
- 대기 표시와 로그에 단계, 대기 조건, 기대값·현재값 및 요청 미전송 여부를 표시하며 동일 상태를 반복 저장하지 않는다.
- Source/Target TrayLoad, ProcessStart, ProcessEnd, CellTrackOut, Target TrayUnload 요청에는 이전 응답값·revision 판정 및 요청 전 Response=0 대기를 사용하지 않는다.
- 요청 후 현재 Response 값으로 판정한다. 정상모드에서는 결과 처리 후 Request=OFF → Response=0 대기를 유지한다. Cycle Test에서는 최종 Response=0 대기를 생략한다.
- 결과 대기 오류의 재시도와 이미 결과를 받은 뒤 초기화 대기 오류의 복구를 구분한다. 초기화 대기 복구에서는 완료된 요청을 다시 보내지 않는다.

향후 수정 시 이 버전을 사용자가 정상 동작을 확인한 비교 기준으로 삼는다. 이 기록만으로 전체 인터락·모든 오류 복구 조건이 검증되었다고 확대 해석하지 않는다.
